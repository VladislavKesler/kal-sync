namespace kal_sync.Models;

/// <summary>What the watch's generic "Cardio" profile actually records — decides the backend formula.</summary>
public enum CardioSport
{
    TennisSingles,
    TennisDoubles,
    Generic,
}
